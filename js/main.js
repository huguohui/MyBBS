// Require configure.　
require.config({
	baseUrl : 'js'
});


// Loads js modules.
require(['jquery'], function($) {
	
});


// Defines a module by a object for some configuration.
define({
	color : 'black',
	size : 'unsize'
});





